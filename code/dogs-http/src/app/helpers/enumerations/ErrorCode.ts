/**
 * Enumeration of all possible errors that may occur.
 * Note: With this enumeration all errors are centered, which is better for their handling and processing.
 */
 export enum ErrorCode {
    UNEXPECTED_BEHAVIOUR = "UnexpectedBehaviorError",
    TIMEOUT = "TimeoutError"
}